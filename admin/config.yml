# Use DecapBridge PKCE auth (required)
backend:
  name: git-gateway
  repo: Alfiandi-ux/Alfiandi
  branch: main
  auth_type: pkce
  base_url: https://auth.decapbridge.com
  auth_endpoint: /sites/addda0f6-73fc-4e06-8650-8a2949633fc9/pkce
  auth_token_endpoint: /sites/addda0f6-73fc-4e06-8650-8a2949633fc9/token
  gateway_url: https://gateway.decapbridge.com

  # See who did what (optional)
  commit_messages:
    create: Create {{collection}} "{{slug}}" - {{author-name}} <{{author-login}}> via DecapBridge
    update: Update {{collection}} "{{slug}}" - {{author-name}} <{{author-login}}> via DecapBridge
    delete: Delete {{collection}} "{{slug}}" - {{author-name}} <{{author-login}}> via DecapBridge
    uploadMedia: Upload "{{path}}" - {{author-name}} <{{author-login}}> via DecapBridge
    deleteMedia: Delete "{{path}}" - {{author-name}} <{{author-login}}> via DecapBridge
    openAuthoring: Message {{message}} - {{author-name}} <{{author-login}}> via DecapBridge

# Enable PKCE fields (optional, recommended)
auth:
  email_claim: email
  first_name_claim: first_name
  last_name_claim: last_name
  avatar_url_claim: avatar_url

# Better Decap + Bridge logo (optional)
logo_url: https://decapbridge.com/decapcms-with-bridge.svg

# Site links in Decap CMS
site_url: https://alfiandi-ux.github.io
display_url: https://alfiandi-ux.github.io

media_folder: "assets/images/uploads"
public_folder: "/assets/images/uploads"

publish_mode: editorial_workflow

collections:
  - name: "posts"
    label: "Tulisan"
    label_singular: "Tulisan"
    folder: "_posts"
    create: true
    slug: "{{year}}-{{month}}-{{day}}-{{slug}}"
    identifier_field: title
    fields:
      - { label: "Judul", name: "title", widget: "string" }
      - { label: "Tanggal", name: "date", widget: "datetime" }
      - label: "Kategori"
        name: "category"
        widget: "select"
        options: ["Tulisan", "Opini", "Pemikiran"]
      - { label: "Ringkasan", name: "excerpt", widget: "text", required: false }
      - { label: "Gambar Sampul", name: "image", widget: "image", required: false }
      - { label: "Isi Tulisan", name: "body", widget: "markdown" }
